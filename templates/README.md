# templates/

Source templates rendered into generated outputs. **Skeleton — populated in M4.**

Planned: the single per-video player page template (one template → N static HTML
pages with per-video `og:` tags), rendered at finalize. See [`SPEC.md`](../SPEC.md) §6.
