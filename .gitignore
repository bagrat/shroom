# dependencies / build
node_modules/
dist/
build/

# OS / editor cruft
.DS_Store
*.swp

# local secrets & runtime (never commit) — see SPEC.md §9
.shroom/
*.env
.env*

# scratch recordings / segments during dev
*.m4s
*.ts
*.m3u8
preview.mp4
