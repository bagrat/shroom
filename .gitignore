# dependencies / build
node_modules/
dist/
build/

# OS / editor cruft
.DS_Store
*.swp

# local secrets & runtime (never commit) — see SPEC.md §9
.shroom/
*.env
.env*

# wrangler's local logs/cache, created wherever a wrangler command runs
.wrangler/

# scratch recordings / segments during dev
*.m4s
*.ts
*.m3u8
preview.mp4

# vendored hls.js — fetched (pinned + SHA-256 verified), not committed; see scripts/page/vendor/
scripts/page/vendor/hls.min.js
